# 📊 دليل إعداد ربط Google Sheets بنسبة 100%

لضمان عمل الربط بين الموقع وجدول البيانات الخاص بك بشكل مثالي، يرجى اتباع الخطوات التالية بدقة:

## 1. إعداد جدول البيانات (Google Sheet)
1. قم بإنشاء جدول بيانات جديد في Google Sheets.
2. انسخ **ID الجدول** من الرابط (يكون الجزء الموجود بين `/d/` و `/edit`).
3. افتح ملف `config.js` في المشروع وقم بتحديث قيمة `SPREADSHEET_ID` بالمعرف الذي نسخته.

## 2. إعداد السكريبت (Google Apps Script)
1. من داخل جدول البيانات، اذهب إلى **Extensions** > **Apps Script**.
2. احذف أي كود موجود وانسخ محتوى ملف `google-apps-script-final.js` المرفق في المشروع.
3. تأكد من تحديث `SPREADSHEET_ID` داخل السكريبت أيضاً في السطر رقم 4.
4. اضغط على أيقونة الحفظ (Save).

## 3. نشر السكريبت كـ Web App (خطوة حاسمة)
1. اضغط على زر **Deploy** في الأعلى ثم اختر **New deployment**.
2. اختر النوع **Web App**.
3. في خانة "Description" اكتب: `Prism Flux API`.
4. في خانة "Execute as" اختر **Me** (بريدك الإلكتروني).
5. في خانة "Who has access" اختر **Anyone** (هذا ضروري جداً ليعمل الربط من الموقع).
6. اضغط **Deploy**.
7. ستظهر لك نافذة تطلب صلاحيات، اضغط **Authorize access** واقبل الصلاحيات (قد تظهر رسالة تحذير، اضغط Advanced ثم Go to Prism Flux).
8. بعد الانتهاء، سيظهر لك **Web App URL**. قم بنسخه.

## 4. ربط الموقع بالسكريبت
1. افتح ملف `config.js` في المشروع.
2. قم بتحديث قيمة `GOOGLE_APPS_SCRIPT_URL` بالرابط الذي نسخته في الخطوة السابقة.
3. احفظ الملف.

## 5. التأكد من النتائج
عند إرسال تقرير من صفحة "التقارير اليومية":
- سيتم إنشاء ورقة جديدة باسم اليوم (مثلاً: `التقارير_2025-12-31`).
- سيتم إضافة البيانات للورقة اليومية.
- سيتم تحديث ورقة **"الرئيسية"** تلقائياً بالنسب المئوية وحالة الإنجاز.
- ستظهر إشعارات فورية في لوحة التحكم عند وصول تقارير جديدة.

---
**ملاحظة:** إذا قمت بتعديل الكود في Apps Script مستقبلاً، يجب عليك عمل **New Deployment** أو تحديث النسخة الحالية لضمان تفعيل التعديلات.
